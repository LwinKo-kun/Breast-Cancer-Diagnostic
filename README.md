# Breast Cancer Diagnostic Classifier (KNN)

A minimal machine learning inference service using a tuned K-Nearest Neighbors pipeline and FastAPI.

## Quickstart

1. Activate virtual environment:
   ```bash
   source .venv/bin/activate
   pip install -r requirements.txt